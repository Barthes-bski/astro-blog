---
interface Props {
    date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="mt-6">
    {date.toLocaleDateString("es-es",
      {
        year: 'numeric',
        month: 'short',
        day: 'numeric'
      }
    )}  
  </time>